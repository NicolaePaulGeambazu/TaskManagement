import { Wrapper } from '../globalstyle';
import TaskGrid from './TaskGrid/TaskGrid';

const DashBoard = () => {
  return (
    <Wrapper>
      <TaskGrid />
    </Wrapper>
  );
};

export default DashBoard;
